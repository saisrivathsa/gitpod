packages:
  - name: docker
    type: docker
    srcs:
      - "plugin/**"
    argdeps:
      - imageRepoBase
    config:
      dockerfile: Dockerfile
      image:
        - ${imageRepoBase}/proxy:${version}
        - ${imageRepoBase}/proxy:${__pkg_version}
